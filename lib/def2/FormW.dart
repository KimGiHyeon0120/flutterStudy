import 'package:flutter/material.dart';

class FormW extends StatelessWidget {
  const FormW({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      home: Row(
        mainAxisAlignment: MainAxisAlignment.center,
        children: [
          Column(
            mainAxisAlignment: MainAxisAlignment.center,

          )
        ],
      ),
    );
  }
}
